"use client";

/**
 * Default blank screen for any not-yet-built screen.
 * Shown as plain white — same look as a fresh iOS app boot.
 */
export function BlankScreen() {
  return <div className="h-full w-full bg-white" />;
}
